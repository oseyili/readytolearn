export const metadata = { title: "Courses" };

export default function Page() {
  return (
    <main style={{ maxWidth: 980, margin: "0 auto", padding: 24 }}>
      <h1 style={{ fontSize: 28, margin: "0 0 8px" }}>Courses</h1>
      <p style={{ opacity: 0.9, marginTop: 0 }}>Courses index will display learning paths. (API integration comes next once deploy is stable.)</p>
    </main>
  );
}